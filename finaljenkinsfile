node{
  stage('scm checkout'){
    git 'https://github.com/sandeep212/javaclone.git'
  }
  stage('mvn buid'){
    sh 'mvn package'
  }
  stage('email notifications'){
    emailext body: 'job build sucessfully', 
    subject: 'jenkins build', 
    to: 'sandeepchowdary212@gmail.com'
  }
}
